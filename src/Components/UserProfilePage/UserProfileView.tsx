import {ReactElement} from "react";

const UserProfileView = (): ReactElement<string> => {
  return (
    <h1>This is were the User Profile components will go</h1>
  )
}

export default UserProfileView;